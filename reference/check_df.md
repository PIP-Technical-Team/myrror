# Check if the df arguments are valid, makes them into a data.frame if they are a list. Internal function.

Check if the df arguments are valid, makes them into a data.frame if
they are a list. Internal function.

## Usage

``` r
check_df(df)
```

## Arguments

- df:

  data frame
